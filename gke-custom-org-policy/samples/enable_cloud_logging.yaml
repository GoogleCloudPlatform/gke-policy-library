name: organizations/${ORG_ID}/customConstraints/custom.loggingService
resource_types: container.googleapis.com/Cluster
method_types:
  - CREATE
  - UPDATE
condition: resource.loggingService != ""
action_type: ALLOW
display_name: Require Logging Service
description: Require GKE clusters to use logging service.

