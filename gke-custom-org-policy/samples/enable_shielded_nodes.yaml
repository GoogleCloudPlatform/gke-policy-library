name: organizations/${ORG_ID}/customConstraints/custom.shieldedNodes
resource_types: container.googleapis.com/Cluster
method_types:
  - CREATE
  - UPDATE
condition: resource.shieldedNodes.enabled == true
action_type: ALLOW
display_name: Require Shielded Nodes
description: Require GKE Shielded Nodes is enabled.
